name: Dispatcher
on: push

jobs:
  dispatch-event:
    runs-on: ubuntu-latest
    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v1
        with:
          repository: subinsebastian/target_repo_for_dispact_event
          token: ${{ secrets.REPO_ACCESS_TOKEN }}
          event-type: ci-update
